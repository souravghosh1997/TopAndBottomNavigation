import { colors } from './colors';
import { space, lineHeights } from './spacing';
import { sizes } from './size';
import { fonts, fontWeights, fontSizes } from './fornts';

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};
